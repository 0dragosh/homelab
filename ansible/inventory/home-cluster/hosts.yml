---

all:
  children:
    # hosts should match 'host_vars'
    master-nodes:
      hosts:
        nuc1:
        nuc2:
    # hosts should match 'host_vars'
    worker-nodes:
      hosts: {}
    #     k8s-3:
    #     k8s-4:
    #     k8s-5:
