---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: storage
  namespace: networking
spec:
  endpoints:
  - dnsName: "storage.${SECRET_DOMAIN}"
    recordTTL: 180
    recordType: CNAME
    targets:
    - "${SECRET_CLOUDFLARE_TUNNEL_ID_1_1}.cfargotunnel.com"
---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: k8sapi
  namespace: networking
spec:
  endpoints:
  - dnsName: "k8sapi.${SECRET_DOMAIN}"
    recordTTL: 180
    recordType: CNAME
    targets:
    - "${SECRET_CLOUDFLARE_TUNNEL_ID_1_1}.cfargotunnel.com"
---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: bastion
  namespace: networking
spec:
  endpoints:
  - dnsName: "bastion.${SECRET_DOMAIN}"
    recordTTL: 180
    recordType: CNAME
    targets:
    - "${SECRET_CLOUDFLARE_TUNNEL_ID_1_1}.cfargotunnel.com"
