---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-config-v2
  namespace: networking
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
