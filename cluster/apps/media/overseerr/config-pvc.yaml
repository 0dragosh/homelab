---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: overseerr-config-v1
  namespace: media
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
  volumeName: overseerr-config-v1
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: overseerr-config-v1
spec:
  capacity:
    storage: 1Gi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  csi:
    driver: driver.longhorn.io
    fsType: ext4
    volumeAttributes:
      numberOfReplicas: "1"
      staleReplicaTimeout: '30'
    volumeHandle: "${LONGHORN_OVERSEERR_PV_NAME}"
  storageClassName: longhorn
